@extends('layouts.admin', ['title' => $purchaseRequest->pr_no.' | '.company()->display_name])
@section('content')
<section class="bg-pine-50"><div class="mx-auto max-w-4xl px-4 py-8"><a href="{{ route('admin.purchase-requests.index') }}" class="text-sm font-semibold text-pine-700">กลับรายการ PR</a><div class="mt-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-pine-200"><div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><h1 class="text-3xl font-semibold text-ink">{{ $purchaseRequest->pr_no }}</h1><p class="mt-2 text-sm text-pine-700">{{ $purchaseRequest->reason }}</p></div><span class="w-fit rounded-full bg-pine-100 px-3 py-1 text-xs font-semibold text-pine-700">{{ $purchaseRequest->status_label }}</span></div><dl class="mt-6 grid gap-4 sm:grid-cols-2"><div><dt class="text-sm text-pine-700">Material</dt><dd class="mt-1 font-semibold text-ink">{{ $purchaseRequest->material->name }}</dd></div><div><dt class="text-sm text-pine-700">Requested Qty</dt><dd class="mt-1 font-semibold text-ink">{{ number_format((float)$purchaseRequest->requested_qty, 3) }} {{ $purchaseRequest->material->unit }}</dd></div><div><dt class="text-sm text-pine-700">Current Stock</dt><dd class="mt-1 font-semibold text-ink">{{ number_format((float)$purchaseRequest->material->current_stock, 3) }}</dd></div><div><dt class="text-sm text-pine-700">Production Order</dt><dd class="mt-1 font-semibold text-ink">@if($purchaseRequest->productionOrder)<a class="text-pine-700" href="{{ route('admin.production.show', $purchaseRequest->productionOrder) }}">{{ $purchaseRequest->productionOrder->production_order_number }}</a>@else - @endif</dd></div></dl></div></div></section>
@endsection
